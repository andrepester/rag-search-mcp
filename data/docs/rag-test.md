# RAG Test

MCP clients can integrate local MCP servers as tools.

In this setup, the raw documents stay local.
Only the chunks found through retrieval are sent to the configured remote language model.
